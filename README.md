# Telegram_stranger_connect_bot

## How to Run

First add `PYTHONPASS` environrment variable in repository directory  by running:

```bash
export PYTHONPASS=${PWD}
```
Then run:

```bash
python src/main.py
```
